# Same as .gitignore BUT allows secret config files to be uploaded to EAS build servers

# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files — never upload secrets to EAS (use Expo dashboard env vars instead)
.env
.env.local
.env*.local

# typescript
*.tsbuildinfo

# generated native folders
/ios
/android

# EAS / Play Store secrets (not needed at build time)
play-service-account.json
google-services.json
